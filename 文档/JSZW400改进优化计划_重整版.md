# JSZW400 改进优化计划（重整版）

> 本文是新的主计划文档，用于后续执行、分工和验收。  
> 原 `JSZW400改进优化执行计划.md` 保留为历史详案、代码分析和过程记录，不再作为日常推进主入口。

## 0. 文档使用规则

- **已落地**：代码或文档已经进入当前工程，但不等同于现场验收通过。
- **待现场验证**：离线静态检查已完成，仍需要设备现场确认。
- **待实现**：尚未开始或仅有方案/需求记录。
- **下一步计划**：按依赖关系和安全风险排序，不是一次性全部开工。
- 涉及 PLC 输出、设备动作、安全互锁、自动启动的任务，必须先完成变量路径、互锁条件和验收用例，再进入代码。

## 1. 当前总体状态

### 1.1 已落地内容

| 模块 | 已落地内容 | 当前状态 | 仍需处理 |
| --- | --- | --- | --- |
| 半自动工序标准化 | HMI 侧已做显式工序归一化；`lstSemiAuto -> Hmi_iSemiAuto` 已统一打包；首尾挤压旧占位值已合并回显式工序 | 已落地，待继续扩 PLC 执行闭环 | PLC `A_SemiAuto` 挤压/分切状态段还需标准化整理 |
| 液压线性斜坡 | 已新增 `FB_HydLinearRamp`；`DualPump` 已接入目标速度到斜坡输出；试点覆盖翻板和夹钳 | 已落地，待现场整定 | `CNST_HYD_*` 参数需空载/带料整定；后续再考虑迁到 `stFolderPara`/HMI |
| 整机复位 PLC 首批闭环 | 已新增 `eTask_SlitterHome`；分条回原位纳入 `TaskSlitter`；`fbMachine.iWholeRst` 已有保守复位顺序 | 已落地，待现场验证 | 物理复位按钮、出料平台、任意干涉姿态脱困能力需现场确认 |
| 整机复位 HMI 操作闭环 | 手动页 `整机复位` 已改为二次确认；执行中锁定相关按钮；状态写入现有 `PLC消息/richMsgInfo` | 已落地 | 仍缺 PLC 专用完成/失败/超时状态回传 |
| 角度表 HMI 维护 | 设置页 `角度映射` 已补齐新增、删除、编辑保存、下载前校验；`angleAddit.txt` 读写改为全量重写并兼容旧表；补偿输入 TabIndex 已按下翻板、上翻板从上到下整理 | 已落地，待操作回归 | 需在 HMI 实机上用旧表、新表、删除当前表、下载 PLC 四类路径复测 |
| 角色化前置核查 | 已输出负责人、自动化总工、PLC-HMI 集成、HMI、实施、测试、评审、文档等角色工件 | 已完成 | 后续按每个新功能继续补验证记录 |

### 1.2 当前最重要的未闭环点

1. **整机复位 PLC 状态回传待编译/现场验证**  
   PLC/HMI 已补整机复位状态码链路，HMI 不再基于现有回读量推断完成；仍需 TwinCAT 编译和设备现场确认：待命、执行中、完成、失败、超时。

2. **整机复位现场边界未确认**  
   `GVL_IO.In_btnReset` 物理复位按钮是否进入主复位链路、出料平台是否纳入整机复位，仍需现场确认。

3. **PLC 半自动执行段仍需整理**  
   HMI 步骤表已有显式工序基础，但 PLC 侧挤压/分切状态段仍需按“进入工序 -> 下发任务 -> 等待完成 -> 确认状态 -> 清理切步”整理。

4. **后续功能还需继续分批落地**  
   角度表维护已完成首批 HMI 落地；英寸单位已完成落地并通过当前轮检验；多语言已完成首批文本导出、法语/俄语资源导入、运行时入口扩展并通过当前轮页面回归；出料平台、自动启动、多种折弯策略、碰撞计算、钳口下压、送料适配、3D 预览等仍属于待实现功能。

## 2. 需要实现的功能清单

### 2.1 设备安全与动作闭环类

| 编号 | 功能 | 当前状态 | 下一步 |
| --- | --- | --- | --- |
| S01 | 整机复位明确完成/失败状态 | PLC/HMI 状态回传代码已补，待 TwinCAT/现场验证 | 现场确认状态码、工位超时和失败分支是否满足实际设备 |
| S02 | 物理复位按钮闭环 | `GVL_IO.In_btnReset` 已映射，但源码未确认主链路消费 | 现场确认按钮功能，再决定是否接入 `iWholeRst` |
| S03 | 出料平台 | 仅需求记录，代码中未见独立任务通道 | 先确认 IO、机械节拍、交接位置，再设计 PLC/HMI |
| S04 | 钳口下压高度控制 | 仅需求记录 | 定义参数范围、默认值、ADS 下发、动作确认条件 |
| S05 | 液压斜坡现场整定 | 代码已落地 | 现场记录启动斜率、减速斜率、最小启动量、末端冲击 |

### 2.2 半自动与自动流程类

| 编号 | 功能 | 当前状态 | 下一步 |
| --- | --- | --- | --- |
| A01 | PLC 半自动标准工序执行闭环 | HMI 显式工序基础已落地，PLC 状态段仍需整理 | 整理 `fbMachine.A_SemiAuto` 挤压/分切/翻面标准模式 |
| A02 | 多种类折弯步骤生成 | HMI 生成层第一阶段闭环已落地；代码层已接入版型识别、正/反序与颜色面候选、基础可行性验算、几何坐标检查、SWI 风格软/硬碰撞评分摘要、步骤快照、初版习惯评分和运行期人工调整偏好学习 | 继续补真实碰撞边界、更多策略输入和可持久化现场样本；PLC/ADS 下发出口仍保持现有 `CurtOrder.lstSemiAuto -> Hmi_iSemiAuto` 链路 |
| A03 | 自动进料位置全尺寸适应及锥度板进料位置 | 仅需求记录 | 建立送料位置计算规则，覆盖长度、宽度、锥度、基准边和偏移 |
| A04 | 程序自动功能：直接点击自动开始 | 仅需求记录 | 复用步骤表生成与打包出口，加启动前校验和确认 |

### 2.3 HMI 数据、配置与国际化类

| 编号 | 功能 | 当前状态 | 下一步 |
| --- | --- | --- | --- |
| H01 | 角度表新增删减功能 | 已落地，待操作回归 | 用旧 `angleAddit.txt`、新增表、删除表、保存、下载 PLC 路径做 HMI 实机复测 |
| H02 | 英寸显示与输入 | 已完成落地，检验通过 | 设置页可切换 `mm/in`；主要长度输入显示经统一单位层换算，内部和 PLC 仍为毫米；后续仅在新增页面时继续沿用统一单位层 |
| H03 | 俄语、法语界面 | 已完成落地，检验通过 | 已建立 CSV 提取/导入链路，`MainFrm` 与共享 `Strings` 已生成法语/俄语资源；当前轮主要页面布局与语义已完成回归，后续新增页面继续补资源即可 |
| H04 | HMI 复位提示资源清理 | 已有复位消息区文案 | 后续若补 PLC 状态码，同步整理提示码和多语言资源 |

### 2.4 预览、校验与高级功能类

| 编号 | 功能 | 当前状态 | 下一步 |
| --- | --- | --- | --- |
| V01 | 2D 预览稳定化 | 已有规划文档，仍需继续落地 | 统一预览数据模型，特殊工序可视化 |
| V02 | 折弯碰撞计算 | 仅需求记录 | 先做简化几何模型和生成前提示，不替代 PLC 安全互锁 |
| V03 | 3D 图形预览 | 仅需求记录 | 定义数据源、步骤快照、板厚/方向表达，与 2D 预览共用步骤表 |

### 2.5 作业数据、参数与 SWIFolder 借鉴类

本节来自 `文档/SWIFolder_reverse_report.md` 的静态分析结论，只作为 JSZW 后续设计参考，不表示当前工程已经具备这些功能。

| 编号 | 功能 | 当前状态 | 下一步 |
| --- | --- | --- | --- |
| D01 | 作业/订单/客户数据库管理 | SWIFolder 可参考，JSZW 当前未形成统一业务数据库 | 先定义 `作业/订单/客户/材料/颜色/归档/完成统计` 的最小数据模型，再决定继续用文本文件、SQLite 还是 SQL Server |
| D02 | Quick Pick / 模板库 | SWIFolder 有可解析的模板字段，JSZW 当前库文件能力较分散 | 设计 JSZW 模板结构，覆盖折弯列表、客户/材料信息、半自动步骤、锥度参数和版本兼容 |
| D03 | 机器参数点表外置化 | JSZW 已有 `Hmi_*` 数组和 `stFolderPara`，但缺少面向维护的统一参数映射表 | 先生成参数点表文档，明确索引、PLC 字段、单位、范围、默认值、HMI 页面和现场整定责任 |
| D04 | 条码调单、完成统计、标签/PDF 报告 | 仅参考功能，暂不进入设备动作链路 | 如果现场需要生产追溯，先做离线报表和完成记录，不直接影响 PLC 自动流程 |
| D05 | 离线办公室模式 | 仅参考功能 | 将其拆为“离线建单/模板准备/导入到机器”三段，先保证文件兼容和版本标识 |
| D06 | 折弯序列候选评分与可解释失败原因 | 可作为后续步骤生成、碰撞计算和 3D 预览的设计参考 | 后续策略化生成时保留候选动作、状态快照、失败码和人工可读原因，不只返回成功/失败 |

## 3. 后续实施批次

### 批次 1：复位闭环现场确认与 PLC 状态回传

**目标**：把当前复位功能从“可操作 + 保守提示”升级成“PLC 明确闭环 + HMI 明确显示”。

任务：

1. 现场确认 `iWholeRst` 当前顺序是否适合实际设备。
2. 确认 `GVL_IO.In_btnReset` 物理按钮是否要接入同一复位状态机。
3. 确认出料平台是否已经存在并需要参与复位。
4. 在 PLC 增加整机复位状态回传：
   - `0`：待命
   - `1`：请求已接收
   - `2`：执行中
   - `3`：完成
   - `4`：失败/超时
5. HMI 手动页改为显示 PLC 状态码对应消息，不再依赖回读量推断完成。

验收：

- HMI 复位、物理复位、单工位复位的安全结果一致。
- 复位完成、失败、超时均有明确消息。
- 急停、暂停、断气/断压恢复后不会自动继续危险动作。

建议主责：

- 电气自动化总工：复位顺序与安全边界
- PLC-HMI 集成工程师：状态码和 ADS 回传路径
- HMI/SCADA 工程师：消息区显示
- 测试验证工程师：现场用例和证据记录

### 批次 2：PLC 半自动标准工序闭环

**目标**：把挤压、分切、翻面等辅助工序在 PLC 执行层真正标准化。

任务：

1. 整理 `fbMachine.A_SemiAuto` 中挤压、开口挤压、分切、翻面状态段。
2. 每类工序统一成：
   - 进入工序
   - 下发 `stFolderCom.Task*`
   - 等待任务完成
   - 二次确认关键状态
   - 清理并切到下一步
3. 保持 `lstSemiAuto -> Hmi_iSemiAuto` 打包出口不变。
4. 不绕开现有急停、光幕、钳口、翻板、分条互锁。

验收：

- 纯折弯件行为不变。
- 中间挤压、中间分切、翻面件 HMI 列表、预览、PLC 执行顺序一致。
- 暂停、继续、急停恢复后步骤不乱序。

### 批次 3：HMI 数据编辑与资源基础

**目标**：先补低风险 HMI 数据能力，为后续自动生成、多语言和单位切换打基础。

任务：

1. 角度表新增、删除、编辑、保存。
2. 角度表范围校验和旧文件兼容。
3. 清点硬编码文本，统一到资源表。
4. 多语言布局预检查，避免俄语/法语后控件溢出。

验收：

- 旧角度表可读。
- 新增/删除角度不破坏订单生成。
- 中英文切换不影响 PLC 下发值。
- 法语/俄语资源导入后项目可编译，设置页可选择新语言。

### 批次 4：步骤生成、送料适配与自动启动

**目标**：让工艺生成和自动启动形成完整闭环。

任务：

1. 多种折弯步骤生成策略化。
2. 自动进料位置全尺寸适应。
3. 锥度板进料基准和偏移规则。
4. 自动启动入口：不再要求操作员先手动发送到半自动。
5. 参考 SWIFolder 模板字段，把客户、材料、厚度、宽度、锥度、步骤偏好和半自动步骤作为生成输入，而不是只依赖当前页面零散控件。

#### A02 后续目标：版型识别驱动的折弯序列规划器

目标：

- 将当前“画图 + 首尾挤压 + 正逆序 + 颜色正反 -> 单一生产列表”的生成方式，升级为“识别图形版型 -> 多策略候选生成 -> 折弯碰撞/可行性验算 -> 用户习惯评分 -> 推荐生产序列”。
- 外部出口保持不变：最终仍写入 `CurtOrder.lstSemiAuto`，后续仍通过 `PackSemiAutoStepsToPlc()` 打包到 `Hmi_iSemiAuto`，不绕过现有半自动下发链路。
- 首轮只在 HMI 生成层推进，不改 PLC 状态机、不改 ADS 数组协议、不新增数据库。

实施分层：

1. 版型识别层：从 `CurtOrder.pxList`、`lengAngle`、首尾挤压、正逆序、颜色正反、锥度和分切信息识别 `FoldShapeProfile`，记录折边数量、角度方向、首尾挤压、颜色面和可选工艺特征。
2. 多策略生成层：同一输入下生成多条候选，例如标准正序、标准逆序、少翻面、少重新夹取、首挤压优先、尾挤压优先、分切/折弯混合策略。
3. 状态快照层：每个候选步骤都保留当步板型姿态、颜色面、A/B 侧、后挡接触边、翻面/重新夹取状态，为碰撞验算和 2D/3D 预览共用。
4. 碰撞与可行性验算层：先做简化模型，逐步检查后挡距离、已折边与翻板/压料区域、夹取长度、翻面必要性和首尾挤压对后续步骤的影响。
5. 用户习惯评分层：在可行候选中按翻面次数、重新夹取次数、后挡变化平顺性、历史人工调整习惯和现场默认版型偏好选出推荐序列。

当前代码进展：

- 已新增 `FoldShapeProfile`：记录折边数量、正负角方向、最大折弯角、首尾挤压、锥度、分切、边做边分切、板宽、板厚和版型 key。
- 已新增 `FoldSequenceCandidate`：候选中保留策略名、步骤表、失败码、失败说明、碰撞摘要、习惯评分和最终惩罚分。
- 已新增 `FoldStepSnapshot`：逐步记录动作类型、颜色面、内外选择、坐标序号、后挡位置、翻面/重新夹取状态、短夹取风险、折后轮廓高度和间隙裕量。
- 已接入基础可行性验算：空步骤、PLC 半自动表容量、未知动作类型、short 打包范围、抓取类型范围和版型坐标范围不满足时，候选会被拒绝，不写入 `CurtOrder.lstSemiAuto`。
- 已接入 SWI 风格碰撞评分骨架：将候选校验分成硬拒绝、软惩罚和 near-collision cubic penalty 三类；后挡超出版型宽度、折后轮廓高度接近夹钳/翻板区域、短夹取等进入候选评分，但不替代 PLC/现场安全互锁。
- 已接入初版候选评分：非边做边分切场景生成当前顺序、反向顺序、颜色面反向、反向顺序+颜色面反向四类候选，按碰撞惩罚、翻面次数、重新夹取次数、后挡变化量、短夹取风险和标准策略习惯加分选出推荐序列。
- 已接入候选可解释摘要：候选现在会保留结构化失败摘要或推荐摘要，成功生成后可沿现有运行时消息区看到选中策略的步骤数、翻面/重抓次数、短夹取风险、软碰撞/near-collision 计数、最小间隙和评分摘要，便于后续人工复核与预览联动。
- 已参考 `进口数控折弯机资料/SWIFolder_reverse_report.md` 补入一批 HMI 侧机械代理阈值：`ClampGap=35`、`PreferredHalfClampHeight=25`、夹钳侧约束带 `15`、反侧软包络 `250`、硬包络 `300`、`MaxOvergripHeight=10`、near-collision 忽略阈值 `0.1`；当前仍是离线评分代理，不替代 PLC/现场安全互锁。
- 已将反侧/对侧近限 soft penalty 拆成独立分支：在夹钳侧约束带内，`250..300` 的正侧高度和 `-300..-250` 的反侧高度不再共用同一布尔条件，便于后续分别细化为 SWI 报告里的 `code 10` 与 `code 8` 近限代理。
- 已将 `FoldCollisionEvaluation` 整理为统一结果对象层，显式承载 `PrimaryCode`、`HardReject`、`ScoreDelta`、`Margin`、`RetrySuggested`；当前仅记录 `RetrySuggested` 字段，不触发镜像重跑或 PLC/ADS 契约变化。
- 已补入 `code 1` 风格的后挡低间隙软惩罚代理：当推进后的轮廓点落到后挡远端低间隙区域时，候选增加 `BackGaugeLowClearancePenalty` 和 `1,000,000` 评分惩罚；后挡远端边界使用 `Hmi_rArray[110]` 的后挡最大位置并保留 SWI `1000.0` 下限，低间隙高度暂按 `BackgaugeHeight = 13.0`。
- 已补入 `code 9` 风格的镜像重试信号第一步：当折弯/挤压步骤在内外侧、颜色面或折弯方向切换上下文中触发硬包络拒绝时，结果对象会记录 `MirrorRetrySuggested` 与 `RetrySuggested=true`；当前仅记录建议，不克隆状态、不镜像重跑。
- 已将当前 near-collision 的 cubic penalty 整理为 `code 7` 风格的正向 `Margin` 输出：`FoldCollisionEvaluation.Margin` 现在承载 near-collision margin，`MinClearanceMargin` 继续保留最小间隙；推荐摘要会同时显示 `margin` 和 `minClearance`。
- 已接入二级 margin helper 框架：`EvaluateSecondaryClearanceMargin()` 会在主碰撞判定后扫描相邻 `FoldStepSnapshot` 的 clearance 窗口，只有超过直接 near-collision margin 时才追加 `SecondaryClearanceMargin`，避免重复计分；后续可把输入替换为真正跨候选/邻接几何池。
- 已将最终候选评分整理为 `FoldScoreBreakdown`：可行性惩罚、策略惩罚、翻面/重抓/短夹取/后挡移动代价、习惯分和人工偏好分现在先分项计算，再汇总成 `TotalPenalty`；推荐摘要会显示 `feasibility`、`motion` 和 `backGaugeTravel`。
- 已新增用户可读诊断层：`PrimaryCode` 先映射成“禁止执行/需复核 + 原因 + 建议处理”，运行消息和失败提示先显示操作员可理解内容，`code/margin/penalty/strategy` 等工程字段保留在 `工程:` 后面供调机追溯。
- 已接入运行期人工调整偏好学习：自动设置页中上移/下移、内外选择、折弯方向、抓取类型、松开高度、回弹等人工调整会记录当前版型 `ShapeKey + 步骤签名`；同一运行周期内再次生成候选时，匹配历史调整的候选会获得 `manual` 习惯评分，参与最终推荐。
- 边做边分切仍保持原优先链路，避免复杂分切场景过早切换策略；真实机型碰撞边界、Quick Pick/模板偏好和可持久化现场样本仍作为后续输入。

验收口径：

- 现有标准工单在未启用新候选评分前，生成结果与当前策略化版本一致。
- 每条候选序列都能说明策略名、失败步骤、失败原因或推荐理由。
- 用户手动调整后的顺序可作为后续习惯评分输入，但不直接覆盖安全/碰撞验算。
- 失败候选不得写入 PLC；只有最终选中的可行序列能进入 `CurtOrder.lstSemiAuto`。

验收：

- 自动启动前必须完成步骤表生成、校验、写入 PLC、确认 PLC 可启动。
- 不允许绕过 `PackSemiAutoStepsToPlc()`。
- 送料位置可人工复算。
- 生成失败时必须给出明确原因，例如尺寸越界、夹持不足、锥度参数缺失或 PLC 状态不允许启动。

### 批次 5：设备扩展动作

**目标**：落地出料平台和钳口下压高度等设备动作能力。

任务：

1. 出料平台 IO 点表、PLC 任务、HMI 状态显示。
2. 钳口下压高度参数、下发、显示和动作确认。
3. 与整机复位、自动流程联动。

验收：

- 每个新动作都有到位确认、超时处理、失败提示。
- 安全互锁不被 HMI 直接写位绕开。

### 批次 6：预览、碰撞与 3D

**目标**：提升生成前检查和操作员预览能力。

任务：

1. 统一 2D 预览数据源。
2. 折弯碰撞计算，先作为生成前提示。
3. 3D 图形预览，复用最终步骤表和 2D 数据源。
4. 为每个候选步骤保留动作状态、几何状态、校验结果码和失败原因，便于解释预览或碰撞提示。

验收：

- 预览错误不得影响 PLC 下发。
- 碰撞提示不能替代 PLC 安全互锁。
- 3D 与 2D 对同一步骤的工序解释一致。
- 禁止用渲染结果反推 A/B 归属、翻面方向或挤压侧；必须从步骤状态和显式标志正向生成。

### 批次 7：单位切换与国际化版本

**目标**：面向海外版本整理单位和语言。

任务：

1. HMI 长度显示和输入切到英寸。
2. 内部计算、PLC 协议、现场参数继续保持毫米。
3. 新订单/库文件增加单位标识，旧文件默认毫米兼容。
4. 俄语、法语资源补齐并做布局回归。

验收：

- `1.000 in` 输入后内部值为 `25.4 mm`。
- PLC 收到的毫米协议值不因界面单位变化而改变。
- 多语言切换后按钮、表格、弹窗不遮挡、不溢出。

### 批次 8：作业数据库、模板库与生产记录

**目标**：把当前文件型订单/库能力升级为可追溯、可导入、可统计的生产数据层，但不抢在设备动作闭环之前影响 PLC 流程。

任务：

1. 建立 JSZW 最小业务数据模型：作业、订单、客户、材料、颜色、模板、完成记录、归档记录。
2. 设计 Quick Pick 类模板结构，保留客户/材料元数据、折弯列表、锥度参数、半自动步骤和版本号。
3. 生成机器参数点表，统一记录 `Hmi_*`、`stFolderCom`、`stFolderPara` 与 HMI 页面之间的关系。
4. 评估条码调单、标签/PDF 报告和完成统计是否进入现场版本。
5. 如果需要办公室模式，先做离线建单和导入校验，不直接操作 PLC。

验收：

- 旧订单和旧库文件可继续读取。
- 新模板能导出、导入、重启后保持一致。
- 数据层错误不会写入 PLC，不会触发自动动作。
- 参数点表能被现场调试人员按索引、单位和范围复核。

## 4. 功能任务卡

### 4.1 整机复位明确状态回传

目标：

- PLC 明确告诉 HMI 当前整机复位状态，不再让 HMI 用位置回读推断。

当前已落地（2026-04-24）：

- PLC `fbMachine` 新增 `iWholeRstStatus`，通过 `Hmi_iArray[23]` 回传整机复位状态：`0` 待命、`1` 请求已接收、`2` 执行中、`3` 完成、`4` 失败/中断、`5` 超时。
- HMI 手动页 `SubOPManual` 改为消费 `Hmi_iArray[23]`，执行中继续锁定整机复位和相关单工位复位按钮；完成、失败、超时只按 PLC 状态码写入 `PLC消息/richMsgInfo`。
- 已同步 `Strings` 中英文、法语、俄语资源和 `文档/localization/jszw1000a-texts.csv`，完成/超时文案已从“离线判定”改为“PLC 确认”。
- 离线验证已通过：`dotnet build .\HMI\JSZW1000A\JSZW1000A\JSZW1000A.csproj -c Debug` 结果 `0 warning / 0 error`；PLC `.TcPOU/.TcDUT` XML 结构检查通过。当前 PATH 未找到 `MSBuild.exe`，尚未做 TwinCAT 编译。

建议文件：

- `26.4/Tc_JSZW400/JSZW/Untitled1/POUs/fbMachine.TcPOU`
- `HMI/JSZW1000A/JSZW1000A/SubWindows/SubOPManual.cs`
- `HMI/JSZW1000A/JSZW1000A/Strings.resx`
- `HMI/JSZW1000A/JSZW1000A/Strings.en-US.resx`

实现要点：

- PLC 增加状态码或明确布尔回传。
- HMI 只在状态变化时写入 `PLC消息/richMsgInfo`。
- 保留当前确认框和执行锁定。

SWIFolder 参考结论：

- `SWIFolder_reverse_report.md` 仅作为参考文档，不承载 JSZW400 的计划内容。
- SWIFolder 的 homing / self-reset 更接近“命令发起 + 模式切换 + UI 锁定 + 周期轮询确认 + 条件满足后写回清理”的闭环，不是一次性按钮动作。
- 其流程明确要求离开当前运行模式并切到 `MANUAL`，必要时临时切到安全机型/规格模式。
- homing 期间正常 UI 控件禁用，完成与失败由轮询到的状态位和位置回读共同驱动。
- 失败不是只弹报警；apron/backgauge homing 失败可进入“重置并重新生成折弯序列”的恢复分支。
- 扩展机构路径受 profile/config gate 控制，例如 loading table、`MiscControlWord2` 等，不应把所有现场可选机构硬写进同一复位流程。

推荐复位流程：

1. 统一复位命令入口：HMI `Hmi_bArray[41]` 与经现场确认的物理复位按钮进入同一 PLC 状态机。
2. 运动前先收敛模式：离开当前运行模式，强制进入 `MANUAL`；若后续确认有轻型/重型等安全回原点差异，再增加临时安全模式切换。
3. 锁定正常 UI：复位期间保留状态显示和必要确认路径，禁止并行单工位复位命令打断。
4. 执行有序复位动作：后挡、台面、送料、分条、翻板、夹钳按现场确认后的顺序回安全位。
5. PLC 全程发布明确状态：HMI 只显示 PLC 状态，不再用本地回读量推断完成。
6. 通过明确分支退出：完成、失败、超时都要有不同状态码和不同消息。

推荐状态码：

| 状态码 | 含义 | HMI 行为 |
| --- | --- | --- |
| `0` | 待命 | 按钮可用，不追加复位消息 |
| `1` | 命令已接收 | 锁定复位相关按钮，提示请求已接收 |
| `2` | 执行中 | 保持锁定，提示复位执行中 |
| `3` | 完成 | 解除锁定，提示 PLC 确认复位完成 |
| `4` | 失败 | 解除锁定，提示互锁/姿态不满足或工位失败，保持手动安全状态 |
| `5` | 超时 | 清活动复位任务，解除锁定，提示 PLC 确认复位超时 |

推荐 PLC 状态机形态：

- `0 Idle`：等待统一复位命令。
- `10 CommandAccepted`：清理冲突任务，发布状态 `1`。
- `20 ModeConverged`：确认手动/安全模式，必要时执行临时安全模式切换。
- `30 InterlocksChecked`：检查当前姿态和互锁，不满足则进入失败。
- `40..90 OrderedUnitReset`：执行现有有序工位复位，并持续发布状态 `2`。
- `100 Completed`：发布状态 `3`，释放 HMI 锁定，回到待命。
- `110 Failed`：发布状态 `4`，释放 HMI 锁定，保持手动安全状态等待人工恢复。
- `120 TimedOut`：发布状态 `5`，清理复位任务，释放 HMI 锁定，保持手动安全状态。

现场边界：

- 物理复位按钮最终口径需现场确认：只监视、短脉冲触发，或长按 1 秒触发同一状态机。
- 出料平台、上料/装载台等扩展机构是否纳入整机复位，应按实际机型和配置门控处理。
- 若存在轻型/重型或等价安全模式差异，需现场确认复位期间是否临时切换。
- 复位失败后是否只停在手动安全状态，或是否触发 HMI 侧步骤/序列重生成，应作为后续策略项单独确认。

对照 SWIFolder 当前仍需完善：

- `GVL_IO.In_btnReset` 物理复位按钮尚未真正并入 `iWholeRst` 同一状态机；目前仍停留在“已映射、待现场确认”的状态。
- 当前 HMI 只是限制“仅手动模式可发起整机复位”；PLC 侧还未像 SWIFolder 那样明确执行“离开当前运行模式并收敛到 `MANUAL`”，也未覆盖必要时临时切到轻型/安全模式。
- 当前失败/超时状态只有总状态码 `4/5`，但还没有工位级失败原因、失败步骤号或互锁类别，HMI 还不能直接区分是后挡、台面、送料、分条、翻板还是钳口导致失败。
- 当前 `iWholeRst` 顺序是固定流程，尚未按机型/配置门控扩展机构；若后续接入出料平台、上料台或其它可选机构，需要像 SWIFolder 的 `loading table` / `MiscControlWord2` 一样通过 profile/config gate 控制，不宜直接硬写进所有机型。
- 当前终态 `3/4/5` 由 PLC 保持短时间后自动回 `0`；后续可评估是否需要增加 HMI 确认清除、命令 token / ack token 或显式复位完成确认，避免 ADS 卡顿时错过终态。
- 当前失败后的处理仍是“提示并停在手动安全状态”；尚未建立 SWIFolder 那种“复位失败后可选清步骤/重置并重新生成折弯序列”的恢复分支。
- 当前 HMI 锁定范围主要覆盖手动页复位相关按钮；后续若要贴近 SWIFolder 的 homing 体验，还需要评估是否应在复位期间统一锁定主导航、半自动/自动启动及其它会并行动作的入口。
- 当前整机复位更接近“回安全姿态”，而不是 SWIFolder 文义上的“重置 backgauge/apron home positions”；现场需要进一步确认 JSZW400 的“自复位”口径是否包含零位重置、回原点或仅包含安全位收敛。

### 4.2 角度表新增删减

目标：

- 操作员可在 HMI 内维护角度表，不依赖手工改文件。

当前已落地（2026-04-24）：

- `MainFrm.angleAddit[]` 继续作为运行时缓存，持久化文件仍为启动目录下 `angleAddit.txt`，每张表保持 35 行结构：表名 1 行、基础参数 4 行、下翻板补偿 15 行、上翻板补偿 15 行。
- 设置页 `角度映射` 已支持新增、删除、编辑保存、下载到 PLC；新增时复制当前选中表的材料、强度、厚度、偏置和 30 个补偿值，删除时保留至少 1 张表并弹出确认。
- 原 `机器测量` 字段已改为 `实测成型角度偏置`：HMI 保存基础补偿和偏置，下载/主界面选表写 PLC 前按 `最终补偿 = 单格补偿 + 实测成型角度偏置` 合成到 `Hmi_rArray[150..164]` 与 `Hmi_rArray[170..184]`。
- 保存/下载前校验表名非空、表名不重复；30 个补偿值、实测成型角度偏置、以及相加后的最终补偿值都必须在 `-180..180` 范围内。
- 角度补偿输入框焦点顺序已整理为：先下翻板 `00..14` 从上到下，再上翻板 `00..14` 从上到下。
- 离线验证已通过：`dotnet build .\HMI\JSZW1000A\JSZW1000A\JSZW1000A.csproj -c Debug`，结果 `0 warning / 0 error`。

建议文件：

- `HMI/JSZW1000A/JSZW1000A/SubWindows/SubOPSetting.cs`
- `HMI/JSZW1000A/JSZW1000A/SubWindows/SubOPSetting.Designer.cs`
- `HMI/JSZW1000A/JSZW1000A/MainFrm.cs`
- 启动目录下 `angleAddit.txt`

实现要点：

- 已完成新增复制当前表、删除、编辑、保存、下载前校验、实测成型角度偏置叠加和旧表读取兼容。
- 仍需现场或实机 HMI 验证：旧表读取、新建表保存后重启、删除当前表、填写偏置后下载 PLC，确认 `Hmi_rArray[150..164]` 与 `Hmi_rArray[170..184]` 等于当前表单格补偿加偏置后的最终值。

### 4.3 出料平台

目标：

- 将出料平台纳入 PLC 控制、HMI 显示、自动流程和复位策略。

前置条件：

- 确认机械是否已安装。
- 确认 IO 点、传感器、执行器、动作节拍。
- 确认与折弯、送料、分条的交接位置。

实现要点：

- 新增任务枚举和 `ST_FolderCom` 字段时必须先确认结构影响。
- 输出必须受急停、位置、夹钳、送料、出料互锁限制。

### 4.4 自动启动入口

目标：

- 操作员可从当前订单/步骤表直接启动自动流程，减少“先发送到半自动再启动”的步骤。

前置条件：

- 步骤表生成稳定。
- 复位闭环稳定。
- PLC 可明确确认接收状态。

实现要点：

- 不绕开现有步骤表打包出口。
- 启动前必须做订单有效性、步骤表有效性、PLC 状态确认。
- HMI 必须二次确认。

### 4.5 英寸单位切换

目标：

- HMI 显示和输入使用英寸，内部和 PLC 保持毫米。

实现要点：

- 统一单位工具层。
- 所有输入边界：英寸 -> 毫米。
- 所有显示边界：毫米 -> 英寸。
- 文件兼容：旧文件默认毫米，新文件写单位。

当前已落地（2026-04-24）：

- 已新增 `DisplayLengthUnit`、`DisplayUnitManager` 和 `MainFrm.Unit.cs`，统一处理 `mm/in` 配置读取、显示格式化和输入解析。
- 程序启动时从 `Config.ini` 读取 `DisplayUnit`；设置页 `显示单位` 可保存 `mm/in`，保存失败会提示操作员检查 `Config.ini`。
- 主界面、自动页、手动页、设置页、分条页和进料窗体中的主要长度显示/输入已改走 `FormatDisplayLength()`、`TryParseDisplayLength()` 或同类封装，PLC/HMI 数组仍保持毫米值。
- 进料窗体已补运行时单位刷新：表格长度/宽度/厚度表头、进料定位单位标签、释放位置 `pos1..pos4` 和长度/宽度越界提示均按当前 `mm/in` 显示，内部计算仍保持毫米。
- 分条页列表行已补真实毫米值缓存：显示文本可随 `mm/in` 和语言刷新重绘，旧行文本会按自带 `mm/in` 单位解析，避免切换英寸后把旧毫米行误按英寸写入 PLC；同时修正从 `Hmi_rSlitter` 回读列表时的行号递增和 8 行上限。
- 自动绘图页 `SubOPAutoDraw` 的网格单元长度和梯形波形尺寸输入已改为按当前显示单位解析；界面上继续显示 `mm/in`，内部绘图比例和生成步骤长度仍保持毫米。
- 工单库页 `SubOPLibrary` 的摘要区长度/厚度已改为按当前显示单位输出；切换目录后内部路径会同步刷新，`重新读取`、删除工单和导入 DXF 生成 `.ini` 均落到当前目录，首条工单也可正常选中后删除。
- 手动页 `SubOPManual` 的订单摘要 `label12` 已改为资源化模板，并继续通过 `FormatDisplayLengthWithUnit()` 输出当前单位，避免运行时残留固定英文 `Name/Length`。
- 离线验证已通过：`dotnet build .\HMI\JSZW1000A\JSZW1000A\JSZW1000A.csproj -c Debug`，结果 `0 warning / 0 error`。

### 4.6 俄语、法语界面资源

目标：

- 将现有 HMI 文本通过统一表格导出、翻译和回写到 `.resx`，避免后续逐页手工维护。

当前已落地（2026-04-24）：

- 已新增导出/导入工具：`tools/localization/resx_translations.py`。
- 已导出翻译表：`文档/localization/jszw1000a-texts.csv`，列结构为 `scope,key,zh-CN,en-US,fr-FR,ru-RU,comment`。
- 已生成法语/俄语资源：
  - `HMI/JSZW1000A/JSZW1000A/MainFrm.fr-FR.resx`
  - `HMI/JSZW1000A/JSZW1000A/MainFrm.ru-RU.resx`
  - `HMI/JSZW1000A/JSZW1000A/Strings.fr-FR.resx`
  - `HMI/JSZW1000A/JSZW1000A/Strings.ru-RU.resx`
- 已扩展运行时语言入口：
  - `AppLanguage` 增加 `FrFr`、`RuRu`
  - `LocalizationManager.GetCulture()` 增加 `fr-FR`、`ru-RU`
  - 设置页 `cbxLanguage` 增加法语、俄语选项
- 进料窗体已补 `Feed.Column.*`、`Feed.Action.*`、`Feed.Label.*` 资源键；分条页法语/俄语 `Slitter.RowTemplate` 与生产状态文本已从误译占位修正为保留 `{0}/{1}/{2}` 的可运行模板。
- 已同步法语/俄语主窗体标题资源；翻译缺失报告为 `fr-FR: 0 missing / ru-RU: 0 missing`。
- 已补自动绘图页新增错误提示资源：网格单元长度、波形数量、正长度输入错误均已同步到 `zh-CN/en-US/fr-FR/ru-RU`。
- 手动页订单摘要新增 `Manual.Label.OrderSummary` 资源键，运行时不再硬编码 `Name:` / `Length:`，法语/俄语也可跟随当前语言切换。
- 手动页步骤表的抓取类型刷新、上移、下移异常弹窗已统一改走 `Strings` 资源模板 `Manual.Error.ActionFailed`，法语/俄语界面下不再直接弹原始异常字符串。
- 分条页 `SubOPSlitter` 的上移/下移异常弹窗已统一改走 `Slitter.Error.ActionFailed`；法语/俄语 `Slitter.Action.MoveUp/MoveDown` 原先串值已修正，操作员界面不再显示错位按钮语义或原始异常字符串。
- 设置页角度表区的新增/删除按钮、数值校验提示和删除确认已统一改走 `Strings` 资源；同步补齐 `zh-CN/en-US/fr-FR/ru-RU` 文案，并修正法语/俄语里这一块原先错位的材料/强度、按钮和提示语义。
- `MainFrm.TryApplyAngleAdditToHmiArrays()` 的角度表无效、实测偏置越界、上下翻板行补偿越界等下载前消息已统一改走 `Strings` 资源，法语/俄语不再退回中英双语硬编码。
- 主窗体 `单工/双工/轻型/重型` 模式区已补运行时覆盖文案层：`单工/双工` 在非中文语言下不再直接暴露中文贴图文字，`轻型/重型` 在法语/俄语下不再停留英文贴图文字，四类模式名统一改走 `Strings` 资源。
- 离线编译验证已通过：`dotnet build HMI\JSZW1000A\JSZW1000A.sln -c Debug`，结果 `0 warning / 0 error`。

剩余工作：

- 翻译缺口报告已排除运行时会覆盖的设计期消息占位，并补齐窗体标题；后续重点从“缺不缺文本”转为“翻译语义是否对齐控件”。
- 子页面 `.fr-FR.resx` / `.ru-RU.resx` 尚未批量生成，原因是当前 CSV 中子页面目标语言列没有非空翻译内容。
- 需要实机或至少 HMI 运行截图回归：主界面按钮、设置页语言框、弹窗、表格列头、长文本是否被法语/俄语撑开或遮挡。

### 4.7 作业/订单/客户数据库与模板库

目标：

- 借鉴 SWIFolder 的作业、订单、客户、材料、颜色、归档和统计模型，为 JSZW 建立可追溯的业务数据层。

当前状态：

- JSZW 当前仍以现有订单/库文件和运行时缓存为主。
- `SWIFolder_reverse_report.md` 已确认 SWIFolder 具备 SQL 作业库、Quick Pick 模板、客户/材料字段、完成统计、条码调单和报表/标签能力。
- 该功能暂不进入 PLC 自动动作链路，先作为后续 HMI 数据层规划。

建议文件：

- `HMI/JSZW1000A/JSZW1000A/MainFrm.cs`
- `HMI/JSZW1000A/JSZW1000A/SubWindows/SubOPLibrary.cs`
- `文档/JSZW400改进优化计划_重整版.md`
- 后续新增 `文档/作业数据库与模板结构.md`

实现要点：

- 先定义最小数据模型，再决定存储方式。
- 模板必须带版本号和单位标识，旧文件默认按毫米兼容。
- 导入失败、字段缺失、版本不匹配时只提示和阻断，不写入 PLC。
- 条码、PDF、标签、完成统计先作为可选业务功能，不影响设备安全闭环。

### 4.8 折弯序列候选评分与可解释校验

目标：

- 在后续步骤生成、碰撞计算和 3D 预览中，保留每个候选动作为什么可用或不可用的解释信息。

当前状态：

- JSZW 当前已有 2D 预览规则文档，强调从步骤状态正向生成。
- SWIFolder 报告显示其折弯规划更接近“候选动作 + 几何状态 + 可行性校验 + 评分选择”的结构，可作为后续策略化参考。

建议文件：

- `HMI/JSZW1000A/JSZW1000A/MainFrm.SemiAuto.cs`
- `HMI/JSZW1000A/JSZW1000A/MainFrm.SemiAuto.DerivedState.cs`
- `HMI/JSZW1000A/JSZW1000A/SubWindows/SubOPAutoView.cs`
- `文档/折弯预览生成规则.md`

实现要点：

- 每个候选步骤保留输入参数、状态快照、校验结果码、失败原因和可人工复核的关键几何量。
- 翻面、A/B 归属、颜色面、挤压图元都必须来自显式状态，不允许由渲染后的图形反推。
- 失败原因至少区分尺寸越界、夹持不足、互锁不满足、参数缺失和未知错误。
- 先服务于离线生成和提示，不替代 PLC 安全互锁。

基于 SWI 程序结构的下一轮整理计划：

- 先按程序结构理解 SWI，而不是继续零散堆规则。当前从 `SWIFolder_reverse_report.md` 已确认的主链路更接近：候选动作构造 -> 轮廓/锚点几何预处理 -> 机械可行性判定 -> 镜像重试 -> 二级间隙 margin helper -> 候选总评分 -> best candidate 选择。
- JSZW 当前 `MainFrm.SemiAuto.cs` 已有候选层、几何推进层和简化评分层，但仍缺少 SWI 风格的“结果对象层”和“镜像重试层”。后续整理时应优先补结构边界，不先继续扩更多候选策略。

建议按以下顺序推进：

1. **结果对象层整理**
   - 已落地第一步：`FoldCollisionEvaluation` 现在明确承载 `PrimaryCode / HardReject / ScoreDelta / Margin / RetrySuggested`，现有 `Penalty` 与软/硬计数仍保留以兼容当前评分链路。
   - 将当前 `FoldCollisionEvaluation` 逐步整理成更接近 SWI 的结构，至少明确承载：
   - `PrimaryCode`
   - `HardReject`
   - `ScoreDelta`
   - `Margin`
   - `RetrySuggested`
   - 目标是让后续 `code 1 / 7 / 9` 等分支能落在统一结果对象上，而不是散在多个 `if` 里。

2. **图形判断依据整理**
   - 明确 JSZW 当前碰撞判断使用的“图形”是 `CurtOrder.pxList` 推进后的折线轮廓，而不是预览渲染结果。
   - 继续把 `ApplyGeometryStep()`、`FlipProfile()`、`IsFlatSideLeft()`、`CalcBackGaugeByCurrentProfile()` 视为“几何状态推进层”，不在这层直接写业务评分。
   - 后续若补 active window、局部锚点区或接触边概念，优先落在这一层。

3. **主机械可行性层补齐**
   - 已落地第一步：`BackGaugeLowClearancePenalty` 作为 `code 1` 代理软惩罚接入结果对象层，当前不硬拒绝，也不触发镜像重试。
   - 先补 `code 1` 风格的 `backgauge low-clearance penalty`，把 SWI 里的 `BackgaugeHeight = 13` 与 JSZW 本地 `BackgaugeApron` / `Hmi_rArray[110..]` 对齐。
   - 保持现有 `ClampGap = 35`、`PreferredHalfClampHeight = 25`、`15 / 250 / 300 / 10 / 0.1` 这些 HMI 侧代理阈值，但继续拆分为独立 code，而不是只保留 hard/soft 汇总。

4. **镜像重试层**
   - 已落地第一步：`RetrySuggested` 会在疑似方向/侧别不匹配导致的硬包络拒绝时记录为 `true`，摘要中保留 `MirrorRetrySuggested`；当前不改变候选搜索，只为后续真正 mirror retry 铺路。
   - SWI 的 `code 9` 不是单纯失败，而是“失败并建议 mirror retry”。
   - JSZW 后续应先记录 `RetrySuggested`，再决定是否把当前 opposite-side 候选扩展成真正的“当前候选失败后按镜像状态重跑一次”链路。
   - 这一步先留在 HMI 生成层，不改 PLC/ADS 出口。

5. **二级 margin helper**
   - 已落地过渡步骤：先把当前单候选 near-collision deficit 对象化为 `Margin` 输出，并把 cubic penalty 公式收敛到独立 helper；真正跨候选/邻接几何的 `0x1000BD60` 风格 helper 仍作为后续步骤。
   - 已落地框架步骤：`EvaluateSecondaryClearanceMargin()` 从 `EvaluateFoldCollision()` 尾部独立出来，当前使用相邻 `FoldStepSnapshot` 做保守窗口扫描，并显式防止重复直接 margin；下一步可接入候选池/active window。
   - SWI 的 `0x1000BD60` 属于“主碰撞判定通过后，再跨候选/邻接几何求 margin”的二级 helper。
   - JSZW 当前 near-collision 仍是单候选内部简化代理，后续若要贴近 SWI，应单独加一层 margin helper，而不是直接在 `EvaluateFoldCollision()` 里继续堆判断。

6. **总评分汇总层**
   - 已落地第一步：`FoldScoreBreakdown` 将 feasibility、strategy、flip/regrip/short-grip/backgauge-travel、habit、manual 分项汇总为 `TotalPenalty`，`CalculateFoldScoreBreakdown()` 只做总分组合，不新增几何判定。
   - 后续继续保持 `CalculateFoldScoreBreakdown()` 只做总分汇总，不再承担几何合法性判定。
   - 可逐步接入：
   - feasibility penalties
   - near-collision cubic penalties
   - grip / short-grip 风险
   - regrip / flip / backgauge travel
   - 但仍以“低分优先、硬拒绝剔除”为主线。

7. **用户提示层**
   - 已落地第一步：`PrimaryCode` 不再直接作为用户提示主体，而是先转成风险等级、用户原因和建议动作；工程摘要保留在 `工程:` 后面。
   - 后续若要做 UI 优化，可把 `工程:` 详情折叠到调试模式，普通操作员只看风险和处理建议。

本轮明确暂不做：

- 不改 PLC 状态机。
- 不改 ADS 数组协议和 `CurtOrder.lstSemiAuto -> PackSemiAutoStepsToPlc() -> Hmi_iSemiAuto` 出口。
- 不新开页面，不把碰撞提示直接升级为 PLC 安全互锁。
- 不把 SWI 的常量原样照搬为最终机型参数，只把它们作为 HMI 离线评分代理和结构参考。

## 5. 当前推荐执行顺序

1. **整机复位 PLC 明确状态回传**
2. **整机复位现场验证与问题清单**
3. **PLC 半自动标准工序闭环**
4. **角度表实机操作回归**
5. **步骤生成策略化 + 自动进料适配**
6. **自动启动入口**
7. **出料平台 / 钳口下压高度**
8. **碰撞计算 / 3D 预览**
9. **作业数据库 / Quick Pick 模板库 / 生产记录**
10. **折弯序列候选评分与可解释校验**

## 6. 旧文档内容映射

| 旧文档章节 | 在新计划中的位置 |
| --- | --- |
| 任务一：半自动中间插入挤压/分切 | 2.2、3 批次 2、4.4 |
| 任务二：图形预览优化 | 2.4、3 批次 6 |
| 任务三：液压线性优化 | 1.1、2.1 |
| 后续任务四：英寸单位 | 2.3、3 批次 7、4.5 |
| 后续功能需求池 | 2.1-2.4 |
| 工程角色分配与落地计划 | 3、4、5 |
| 整机复位前置核查和已落地记录 | 1.1、1.2、3 批次 1、4.1 |
| SWIFolder 反向分析可借鉴功能 | 2.5、3 批次 8、4.7、4.8 |
