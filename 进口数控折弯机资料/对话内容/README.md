# 对话内容迁移与去重说明

本目录用于存放从当前资料目录迁移出来的对话日志，并提供一份人工校核后的去重结果。

## 目录结构

- `raw_logs/`
  - 原始迁移日志，保留不改。
- `deduped_topics.jsonl`
  - 去重后的推荐入口。
  - 每行 1 条主题级记录，保留来源文件和来源行号，便于追溯。

## 原始来源

- 来源目录：`E:\_Work\JSZW双向智能折边机\JSZW400\进口数控折弯机资料\.omx\logs\`
- 迁移目标：`E:\_Work\JSZW双向智能折边机\JSZW400\进口数控折弯机资料\对话内容\raw_logs\`
- 当前原始文件：
  - `turns-2026-04-25.jsonl`
  - `turns-2026-04-27.jsonl`
  - `turns-2026-04-28.jsonl`

## 去重规则

- 不修改 `raw_logs` 原始数据。
- 先按记录逐条检查 `thread_id`、`input_preview`、`output_preview`。
- 对“同一主题的辅助生成记录”做合并，例如：
  - 先生成标题，再发起真实任务。
  - 标题生成结果已经被后续真实任务覆盖。
- `ambient suggestions` 保留为独立主题，因为它们对应不同后续分析方向，并非重复。

## 本次结果

- 原始记录数：`5`
- 完全重复记录数：`0`
- 主题级去重后记录数：`4`
- 被合并的重复主题：
  - `反编译折弯碰撞信息`
    - 合并来源：`turns-2026-04-25.jsonl` 第 2 行（标题生成）与第 3 行（真实任务）

## 推荐查看方式

1. 先看 `deduped_topics.jsonl`。
2. 需要原始上下文时，再回到 `raw_logs/*.jsonl` 按 `source_refs` 回溯。
